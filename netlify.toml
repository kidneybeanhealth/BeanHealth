[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback only — proxy is handled by the serverless function via config.path
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
